xml.instruct!
xml.surveys do
  @surveys.each do |survey|
    xml.survey do
      xml.survey_id survey.id
      xml.title survey.title
      xml.lat survey.lat
      xml.lng survey.lng
    end
  end
end
